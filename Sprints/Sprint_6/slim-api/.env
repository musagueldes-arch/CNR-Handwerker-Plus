# ============================================================
#  HandwerkerPro REST-API — Umgebungsvariablen
#
#  ANLEITUNG:
#    1. Diese Datei als ".env" kopieren
#    2. Alle Werte anpassen
#    3. .env niemals in Git committen! (.gitignore beachten)
# ============================================================

# ── Anwendung ────────────────────────────────────────────────
APP_ENV=development       # development | production
APP_DEBUG=true            # true nur in Entwicklung!

# ── Datenbank ────────────────────────────────────────────────
DB_HOST=localhost
DB_NAME=handwerkerpro_db
DB_USER=root
DB_PASS= Muhatay@58

# ── API-Keys ─────────────────────────────────────────────────
# Starke, zufällige Keys verwenden! Beispiel generieren:
#   php -r "echo bin2hex(random_bytes(32));"
#
# Entwicklung (Beispielwerte — in Produktion unbedingt ersetzen!):
API_KEY_ADMIN=ersetze-mich-mit-einem-sicheren-zufalls-key
API_KEY_MEISTER=ersetze-mich-mit-einem-sicheren-zufalls-key
API_KEY_GESELLE=ersetze-mich-mit-einem-sicheren-zufalls-key

# ── CORS ─────────────────────────────────────────────────────
# Entwicklung:          CORS_ORIGINS=*
# Sprint 7 (Frontend):  CORS_ORIGINS=http://localhost:3000
# Produktion:           CORS_ORIGINS=https://mein-frontend.de
# Mehrere Origins:      CORS_ORIGINS=https://a.de,https://b.de
CORS_ORIGINS=*

# ── Demo-Passwort ────────────────────────────────────────────
# Nur aktiv wenn APP_ENV=development — niemals in Produktion!
DEMO_PASSWORD=demo123

# ── E-Mail / SMTP ─────────────────────────────────────────
# Entwicklung: Mailtrap (https://mailtrap.io — kostenlos)
# Produktion:  Eigener SMTP-Server oder Dienst (SendGrid, etc.)
MAIL_SMTP_HOST=sandbox.smtp.mailtrap.io
MAIL_SMTP_PORT=587
MAIL_SMTP_USER=dein-mailtrap-user
MAIL_SMTP_PASS=dein-mailtrap-pass
MAIL_SMTP_SECURE=tls
MAIL_FROM_EMAIL=noreply@klausmeier-gmbh.de
MAIL_FROM_NAME=Klaus Meier GmbH
MAIL_DEBUG=false

# SSL-Zertifikatsprüfung beim SMTP-Versand:
#   true  → Zertifikat wird geprüft (Standard, Pflicht in Produktion)
#   false → Prüfung deaktiviert (nur für lokale Entwicklung, z.B. Mailtrap)
#
# ⚠️  SICHERHEITSHINWEIS: Dieser Wert wird in Produktion (APP_ENV=production)
#     vom System auf "true" erzwungen — unabhängig davon was hier steht.
#     "false" hat also NUR in APP_ENV=development eine Wirkung.
MAIL_SSL_VERIFY=false
